import { styled } from "@mui/material";

export const Header = styled('div')({
    display: 'flex', 
    alignItems: 'center', 
    justifyContent: 'flex-end',
    padding: '10px',
    height: '40px'
   });